<!-- <%- include('partials/header') %>
<%- include('partials/navbar') %>

<div class="max-w-4xl mx-auto">
  <h2 class="text-2xl font-bold text-indigo-700 mb-4"><%= folder.name %></h2>

  <form action="/files/upload" method="POST" enctype="multipart/form-data" class="mb-6 bg-white p-4 rounded shadow">
    <input type="hidden" name="folderId" value="<%= folder.id %>" />
    <input type="file" name="file" class="block mb-4" required />
    <button class="bg-green-500 text-white px-4 py-2 rounded">Upload File</button>
  </form>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
    <% files.forEach(file => { %>
      <div class="bg-white p-4 rounded shadow">
        <div class="font-bold"><%= file.name %></div>
        <div class="text-sm text-gray-500">Size: <%= file.size %> KB</div>
        <div class="text-sm text-gray-500">Uploaded: <%= file.createdAt.toLocaleDateString() %></div>
        <a href="<%= file.url %>" class="text-indigo-600 underline mt-2 inline-block">Download</a>
      </div>
    <% }) %>
  </div>
</div>
<%- include('partials/footer') %> -->
