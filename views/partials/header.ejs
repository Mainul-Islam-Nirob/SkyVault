<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>SkyVault </title>

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet" />

  <script src="https://cdn.tailwindcss.com"></script>
  <script>
tailwind.config = {
  theme: {
    extend: {
     colors: {
        neonTeal: '#00FFC8',          // Bright cyan/teal (main highlight)
                midnightNavy: '#121829',      // Dark navy background
        electricCyan: '#00FFC8',      // Same as neonTeal, interchangeable
        softLavender: '#A899E6',      // Muted lavender/purple accent
                darkSlate: '#1E2336',         // Dark slate blue, secondary bg
        primary: '#00FFC8',           // Alias for neonTeal / electricCyan
        accent: '#A899E6',            // Alias for softLavender
        moonlightGray: '#A3A3A3', // Darker gray for text
  lightGray: '#D1D5DB',     // Tailwind's gray-300
  mutedText: '#9CA3AF',     // Tailwind's gray-400
},
      fontFamily: {
        sans: ['Poppins', 'sans-serif'],
      },
      boxShadow: {
        glowCyan: '0 0 8px #00FFC8',
        glowLavender: '0 0 10px #A899E6',
      }
    }
  }
}


  </script>
</head>
<body class="bg-cosmicNavy text-moonlightGray font-sans min-h-screen flex flex-col">
